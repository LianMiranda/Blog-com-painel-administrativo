<nav class="navbar navbar-expand-lg navbar-dark bg-primary p-3 mb-4">
    <a href="/" class="navbar-brand">Ponto e Vírgula</a>

    <% if (!user) { %>
        <ul class="navbar-nav d-flex align-items-center flex-row gap-3">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Categorias
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <% categories.forEach(category => { %>
                        <li><a class="dropdown-item" href="/category/<%= category.slug %>"><%= category.title %></a></li>
                        <% }) %>
                    </ul>
                </li>
            </ul>
            <ul class="navbar-nav d-flex flex-row gap-3 ms-auto">
                <li class="nav-item">
                    <a href="/login" class="nav-link">Login</a>
                </li>
                <li class="nav-item">
                    <a href="/singUp" class="nav-link">Sign up</a>
                </li>
            </ul>
    <% } else { %>
        <ul class="navbar-nav d-flex align-items-center flex-row gap-3">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Categorias
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <% categories.forEach(category => { %>
                        <li><a class="dropdown-item" href="/category/<%= category.slug %>"><%= category.title %></a></li>
                        <% }) %>
                    </ul>
                </li>
            </ul>
            <ul class="navbar-nav d-flex flex-row gap-3 ms-auto">
                <li class="nav-item">
                    <a href="/myArticles" class="nav-link">Meus Artigos</a>
                </li>
                <li class="nav-item">
                    <a href="/articles/new" class="nav-link">Novo Artigo</a>
                </li>
                <li class="nav-item">
                    <a href="/logout" class="nav-link">Logout</a>
                </li>
            </ul>
        <% } %>
</nav>